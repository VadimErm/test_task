{% extends 'default/issues.html.twig' %}

{% block content %}
    <h1>Comment edit</h1>
  <div class="row">
   <div class="col-md-4"> 
    {{ form_start(edit_form) }}
    {{ form_errors(edit_form) }}
       <div class="form-group">
           {{ form_label(edit_form.comment) }}
           {{ form_errors(edit_form.comment) }}
           {{ form_widget(edit_form.comment, {'attr': {'class':'form-control'}}) }}
       </div>
       <div class="form-group">
           {{ form_label(edit_form.projectId) }}
           {{ form_errors(edit_form.projectId) }}
           {{ form_widget(edit_form.projectId, {'value': projectId, 'attr': {'class':'form-control'}}) }}
       </div>
       <div class="form-group">
        <input class="btn btn-primary" type="submit" value="Edit" />
       </div>
    {{ form_end(edit_form) }}
    {{ form_start(delete_form) }}
                <input class="btn btn-primary" type="submit" value="Delete">
    {{ form_end(delete_form) }}
    </div>
   </div>
    <ul class="list-unstyled">
        <li>
            <a href="{{ path('comments_show', { 'projectId' : projectId, 'id' : comment.id }) }}">Back to the comment</a>
        </li>
    </ul>
{% endblock %}
