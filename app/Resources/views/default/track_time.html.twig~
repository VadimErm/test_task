{% extends 'default/issues.html.twig' %}

{% block content %}
    
  <div class="row">
     <div class="col-md-4">    
        {{ form_start(form) }}
        {% form_theme form 'default/fields.html.twig' %}
        {{ form_errors(form) }}

       <div class="form-group">
           {{ form_label(form.issue) }}
           
           {{ form_errors(form.issue) }}
           {{ form_widget(form.issue, {'value': issueId,  'attr': {'class':'form-control'}}) }}
       </div>

       <div class="form-group">
           {{ form_label(form.hours) }}
           {{ form_errors(form.hours) }}
           {{ form_widget(form.hours, {'attr': {'class':'form-control'}}) }}
       </div>
       <div class="form-group">
           {{ form_label(form.comment) }}
           {{ form_errors(form.comment) }}
           {{ form_widget(form.comment, {'attr': {'class':'form-control'}}) }}
       </div>
       <div class="form-group">
           {{ form_widget(form.create, {'attr': {'class':'btn btn-primary'}}) }}
       </div>

       {{ form_end(form) }}
     </div>
  </div>
           <ul>
               <li>
                  <a href="{{ path('issue', { 'issueId' : issue.id }) }}">Back to the issue</a>      
               </li>   
             </ul>     
{% endblock %}